
# Wise Leap Technology Assignment

This repository contains the automated tests for the Wise platform’s environment. The tests are written using Python language and the Selenium WebDriver framework. The tests will cover the following scenarios:

1) Perform Login as Tutor
2) Go to the classroom and assert that classroom is opened Successfully
3) Schedule a session 
4) Assert the session and session details


## Prerequisites
Before running the tests, ensure the following are installed on your machine:

- Python (version 3.x)
- Selenium WebDriver
- Google Chrome WebDriver


## Running the Tests
The test script is located in "assesment.py" file. To run the test script, copy the code and run in your local IDE, I have used VSCode. 


